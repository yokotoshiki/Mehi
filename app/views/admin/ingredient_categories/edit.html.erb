<section class="inner">
  <h2>食材カテゴリ管理</h2>

	<%= form_with model: @ingredient_category, url:admin_ingredient_categories_path, local: true do |f| %>
	    <%= f.label :name, "カテゴリ名" %></br>
	    <%= f.text_field :name %>

	    <%= f.submit "追加",class: "btn btn-primary" %>
    <% end %>
</section>